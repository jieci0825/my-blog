/**
 * 创建每日一言
 */
function createDailyQuote(ctx) {}

/**
 * 编辑每日一言
 */
function editDailyQuote(ctx) {}

/**
 * 获取每日一言列表
 */
function getDailyQuoteList(ctx) {}

/**
 * 删除每日一言
 */
function deleteDailyQuote(ctx) {}

module.exports = {
	createDailyQuote,
	editDailyQuote,
	getDailyQuoteList,
	deleteDailyQuote
}
